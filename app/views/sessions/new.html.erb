<h1>Log in</h1>

<%= form_with(url: login_path, scope: :session) do |f| %>
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>

    <%= f.label :password %>
    <%= f.password_field :password %>

    <%= f.submit "Log in" %>
<% end %>